# TODO - Streamlit Deployment (Decision Tree Regressor - Diabetes)

- [ ] Create `model_pipeline.py` with preprocessing + model training logic (IQR outlier removal, correlation-threshold feature selection, scaling, DecisionTreeRegressor).
- [ ] Create `train.py` to train the model and save fitted artifact to `models/decision_tree_diabetes.joblib`.
- [ ] Create `app.py` Streamlit UI to accept 10 numeric inputs and display prediction.
- [ ] Create `requirements.txt`.
- [ ] Create `README.md` with setup + run instructions.
- [ ] Create `.gitignore`.
- [ ] Train once locally (`python train.py`) and run app (`streamlit run app.py`).


[1mdiff --git a/app.py b/app.py[m
[1mindex 72d1df4..bc1808c 100644[m
[1m--- a/app.py[m
[1m+++ b/app.py[m
[36m@@ -1,30 +1,53 @@[m
[31m-import streamlit as st[m
 import joblib[m
[32m+[m[32mimport streamlit as st[m
 import numpy as np[m
[31m-import os[m
[32m+[m[32mimport pandas as pd[m
[32m+[m
 [m
[31m-# Load model with caching[m
 @st.cache_resource[m
[31m-def load_model():[m
[31m-    possible_paths = [[m
[31m-        os.path.join(os.path.dirname(__file__), "model.pkl"),[m
[31m-        os.path.join(os.getcwd(), "model.pkl"),[m
[31m-        "model.pkl"[m
[31m-    ][m
[31m-[m
[31m-    for model_path in possible_paths:[m
[31m-        if os.path.exists(model_path):[m
[31m-            model = joblib.load(model_path)[m
[31m-            return model[m
[31m-[m
[31m-    raise FileNotFoundError("model.pkl not found")[m
[31m-[m
[31m-# Load model[m
[31m-try:[m
[31m-    model = load_model()[m
[31m-except FileNotFoundError:[m
[31m-    st.error("❌ Model file 'model.pkl' not found.")[m
[31m-    st.stop()[m
[31m-except Exception as e:[m
[31m-    st.error(f"❌ Error loading model: {e}")[m
[31m-    st.stop()[m
\ No newline at end of file[m
[32m+[m[32mdef load_artifact():[m
[32m+[m[32m    return joblib.load("models/decision_tree_diabetes.joblib")[m
[32m+[m
[32m+[m
[32m+[m[32mdef main():[m
[32m+[m[32m    st.set_page_config(page_title="Diabetes Regression - Decision Tree", layout="wide")[m
[32m+[m
[32m+[m[32m    st.title("Diabetes Regression (Decision Tree Regressor)")[m
[32m+[m
[32m+[m[32m    try:[m
[32m+[m[32m        artifact = load_artifact()[m
[32m+[m[32m    except FileNotFoundError:[m
[32m+[m[32m        st.error("Model artifact not found. Run `python train.py` first.")[m
[32m+[m[32m        return[m
[32m+[m
[32m+[m[32m    preprocessing = artifact["preprocessing"][m
[32m+[m[32m    model = artifact["model"][m
[32m+[m[32m    feature_names = list(artifact["feature_names"])[m
[32m+[m
[32m+[m[32m    cols = st.columns(2)[m
[32m+[m[32m    with cols[0]:[m
[32m+[m[32m        st.subheader("Input features")[m
[32m+[m
[32m+[m[32m        inputs = {}[m
[32m+[m[32m        for name in feature_names:[m
[32m+[m[32m            # diabetes feature names are numeric features already[m
[32m+[m[32m            inputs[name] = st.number_input(name, value=0.0, format="%.6f")[m
[32m+[m
[32m+[m[32m        predict_btn = st.button("Predict")[m
[32m+[m
[32m+[m[32m    with cols[1]:[m
[32m+[m[32m        st.subheader("Prediction")[m
[32m+[m[32m        if "metrics" in artifact:[m
[32m+[m[32m            m = artifact["metrics"][m
[32m+[m[32m            st.caption(f"Training evaluation: MAE={m['mae']:.3f}, R2={m['r2']:.3f}")[m
[32m+[m
[32m+[m[32m        if predict_btn:[m
[32m+[m[32m            X = pd.DataFrame([inputs], columns=feature_names)[m
[32m+[m[32m            X_proc = preprocessing.transform(X)[m
[32m+[m[32m            y_pred = model.predict(X_proc)[m
[32m+[m[32m            st.success(f"Predicted disease progression (numeric): **{float(y_pred[0]):.4f}**")[m
[32m+[m
[32m+[m
[32m+[m[32mif __name__ == "__main__":[m
[32m+[m[32m    main()[m
[41m+[m
